apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

# patchesStrategicMerge was migrated to the unified patches API.
patches:
  - path: service.yaml
  - path: deployment.yaml
